'use client'

import { Pagination } from '@/features/Pagination'

const ProjectsPagination = () => {
  return <Pagination totalPages={5} currentPage={1} onChange={() => {}} />
}

export { ProjectsPagination }
